# TRIDENT BlueOS Extension - Minimal Dependencies
# Optimized for low resource usage

# Core async web framework
aiohttp==3.9.1
aiohttp-cors==0.7.0

# Numerical operations (lightweight)
numpy==1.26.2

# GPIO/PWM control for servos (Raspberry Pi only)
pigpio==1.78

# Health check support
requests==2.31.0
